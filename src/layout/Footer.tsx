import { HStack } from "@chakra-ui/react";


type Props = {
    
};

const Footer: React.FC<Props> = (props) => {
    const {  } = props;

    return <HStack>footer section</HStack>;
};

export default Footer;